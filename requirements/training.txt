-r serving.txt

mlflow==3.15.2
dvc==3.59.0
pyyaml==6.0.3

# pyarrow >= 19 is required, not merely preferred: pyarrow 18.x initialises DLLs that
# exhaust Windows static-TLS slots, after which `import torch` fails with WinError 1114
# in any process that touches pandas first. mlflow 2.x pins pyarrow<19, which is why
# this project tracks mlflow 3.x.
pyarrow==25.0.1

# DVC 3.59 uses a private pathspec symbol that pathspec 1.x removed; without this pin
# every `dvc` invocation dies with ImportError on _DIR_MARK.
pathspec==0.12.1
